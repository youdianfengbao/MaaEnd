name: Sync JSON Schema Files from MaaFramework

on:
  schedule:
    - cron: '30 22 * * *'  # UTC+8 06:30

  workflow_dispatch:

permissions:
  contents: write

jobs:
  check-repository:
    runs-on: ubuntu-latest
    outputs:
      should_run: ${{ steps.check.outputs.should_run }}
    steps:
      - id: check
        run: |
          if [[ "${{ github.repository }}" == "MaaEnd/MaaEnd" ]]; then
            echo "Repository is MaaEnd/MaaEnd, proceeding with sync"
            echo "should_run=true" >> $GITHUB_OUTPUT
          else
            echo "Repository is not MaaEnd/MaaEnd, skipping sync"
            echo "should_run=false" >> $GITHUB_OUTPUT
          fi

  sync-schema-files:
    needs: check-repository
    if: needs.check-repository.outputs.should_run == 'true'
    runs-on: ubuntu-latest

    steps:
      - name: Check Out Repository
        uses: actions/checkout@v6
        with:
          token: ${{ secrets.MAAEND_BOT_TOKEN }}

      - name: Download JSON schema files from MaaFramework
        run: |
          echo "Current Date and Time (UTC): $(date -u '+%Y-%m-%d %H:%M:%S')"

          # 确保目标目录存在
          mkdir -p tools/schema

          # 下载JSON schema文件
          curl -sfS -o tools/schema/interface.schema.json https://raw.githubusercontent.com/MaaXYZ/MaaFramework/main/tools/interface.schema.json || { echo "::error::Failed to download interface.schema.json"; exit 1; }
          curl -sfS -o tools/schema/interface_config.schema.json https://raw.githubusercontent.com/MaaXYZ/MaaFramework/main/tools/interface_config.schema.json || { echo "::error::Failed to download interface_config.schema.json"; exit 1; }
          curl -sfS -o tools/schema/interface_import.schema.json https://raw.githubusercontent.com/MaaXYZ/MaaFramework/main/tools/interface_import.schema.json || { echo "::error::Failed to download interface_import.schema.json"; exit 1; }
          curl -sfS -o tools/schema/pipeline.schema.json https://raw.githubusercontent.com/MaaXYZ/MaaFramework/main/tools/pipeline.schema.json || { echo "::error::Failed to download pipeline.schema.json"; exit 1; }

          echo "Downloaded files:"
          ls -la tools/schema/*.schema.json

      - name: Check for changes with git status
        id: check_status
        run: |
          # 确保git能看到未跟踪的文件
          git add -N tools/schema/

          # 检查是否有任何更改（修改、新增、删除）
          if [[ -n "$(git status --porcelain tools/schema/)" ]]; then
            echo "Changes detected in JSON schema files"
            git status --porcelain tools/schema/
            echo "changes_detected=true" >> $GITHUB_OUTPUT

            # 检查哪些文件发生了变化
            if [[ -e tools/schema/interface.schema.json ]] && [[ -n "$(git status --porcelain tools/schema/interface.schema.json)" ]]; then
              echo "interface_changed=true" >> $GITHUB_OUTPUT
            else
              echo "interface_changed=false" >> $GITHUB_OUTPUT
            fi
            if [[ -e tools/schema/interface_config.schema.json ]] && [[ -n "$(git status --porcelain tools/schema/interface_config.schema.json)" ]]; then
              echo "interface_config_changed=true" >> $GITHUB_OUTPUT
            else
              echo "interface_config_changed=false" >> $GITHUB_OUTPUT
            fi
            if [[ -e tools/schema/interface_import.schema.json ]] && [[ -n "$(git status --porcelain tools/schema/interface_import.schema.json)" ]]; then
              echo "interface_import_changed=true" >> $GITHUB_OUTPUT
            else
              echo "interface_import_changed=false" >> $GITHUB_OUTPUT
            fi
            if [[ -e tools/schema/pipeline.schema.json ]] && [[ -n "$(git status --porcelain tools/schema/pipeline.schema.json)" ]]; then
              echo "pipeline_changed=true" >> $GITHUB_OUTPUT
            else
              echo "pipeline_changed=false" >> $GITHUB_OUTPUT
            fi
          else
            echo "No changes detected in JSON schema files"
            echo "changes_detected=false" >> $GITHUB_OUTPUT
            echo "interface_changed=false" >> $GITHUB_OUTPUT
            echo "interface_config_changed=false" >> $GITHUB_OUTPUT
            echo "interface_import_changed=false" >> $GITHUB_OUTPUT
            echo "pipeline_changed=false" >> $GITHUB_OUTPUT
          fi

      - name: Build commit message
        id: build_msg
        if: steps.check_status.outputs.changes_detected == 'true'
        run: |
          changed_files=""
          if [[ "${{ steps.check_status.outputs.interface_changed }}" == "true" ]]; then
            changed_files+="interface "
          fi
          if [[ "${{ steps.check_status.outputs.interface_config_changed }}" == "true" ]]; then
            changed_files+="interface_config "
          fi
          if [[ "${{ steps.check_status.outputs.interface_import_changed }}" == "true" ]]; then
            changed_files+="interface_import "
          fi
          if [[ "${{ steps.check_status.outputs.pipeline_changed }}" == "true" ]]; then
            changed_files+="pipeline "
          fi
          changed_files=$(echo "$changed_files" | xargs)
          echo "msg=chore: update $changed_files schema files from MaaFramework" >> $GITHUB_OUTPUT

      - name: Commit and push changes
        if: steps.check_status.outputs.changes_detected == 'true'
        uses: actions-js/push@master
        with:
          rebase: true
          branch: ${{ github.ref }}
          message: "${{ steps.build_msg.outputs.msg }}

            Triggered by ${{ github.sha }}

            [skip changelog]"
          github_token: ${{ secrets.MAAEND_BOT_TOKEN }}

      - name: No changes detected
        if: steps.check_status.outputs.changes_detected != 'true'
        run: echo "No changes detected in JSON schema files"
