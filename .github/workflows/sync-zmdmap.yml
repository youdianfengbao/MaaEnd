name: Sync ZmdMap Data

on:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  sync-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          token: ${{ secrets.MAAEND_BOT_TOKEN }}
          ref: ${{ github.event.repository.default_branch }}

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: |
          pip install -r tools/map_tracker/requirements.txt

      - name: Download JSON data
        run: |
          python3 -u tools/map_tracker/map_fetcher.py json -o assets/data/ZmdMap

      - name: Get version info
        id: version
        run: |
          readarray -t VER < <(python3 tools/map_tracker/map_fetcher.py version -i assets/data/ZmdMap/version.json)
          echo "version=${VER[0]}" >> "$GITHUB_OUTPUT"
          echo "game_version=${VER[1]}" >> "$GITHUB_OUTPUT"
          echo "res_version=${VER[2]}" >> "$GITHUB_OUTPUT"

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v8
        with:
          token: ${{ secrets.MAAEND_BOT_TOKEN }}
          add-paths: assets/data/ZmdMap
          commit-message: "chore: sync zmdmap data ${{ steps.version.outputs.version }}"
          branch: chore/sync-zmdmap-data
          title: "chore: sync zmdmap data ${{ steps.version.outputs.version }}"
          body: |
            ## Summary

            This PR automatically synchronized data files from zmdmap.

            Please review the changes before merging it.

            **Version Information:**
            - Version: ${{ steps.version.outputs.version }}
            - Game Version: ${{ steps.version.outputs.game_version }}
            - Resource Version: ${{ steps.version.outputs.res_version }}

  sync-pipeline-generate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          token: ${{ secrets.MAAEND_BOT_TOKEN }}
          ref: ${{ github.event.repository.default_branch }}

      - uses: pnpm/action-setup@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: "pnpm"

      - name: Install dependencies
        run: |
          pnpm install --frozen-lockfile

      - name: Generate pipeline data
        run: |
          pnpm generate:EnvironmentMonitoring
          pnpm generate:SellProduct

      - name: Get version info
        id: version
        run: |
          echo "version=$(cat tools/pipeline-generate/data/version.txt)" >> "$GITHUB_OUTPUT"

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v8
        with:
          token: ${{ secrets.MAAEND_BOT_TOKEN }}
          add-paths: |
            tools/pipeline-generate
            assets
          commit-message: "chore: sync zmdmap pipeline data ${{ steps.version.outputs.version }}"
          branch: chore/sync-zmdmap-pipeline-data
          title: "chore: sync zmdmap pipeline data ${{ steps.version.outputs.version }}"
          body: |
            ## Summary

            This PR automatically synchronized zmdmap data used by pipeline-generate and regenerated dependent artifacts.

            Please review the changes before merging it.

            **Generated Tasks:**
            - EnvironmentMonitoring
            - SellProduct

            **Version Information:**
            - Version: ${{ steps.version.outputs.version }}

  sync-map:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          token: ${{ secrets.MAAEND_BOT_TOKEN }}
          ref: ${{ github.event.repository.default_branch }}

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: |
          pip install -r tools/map_tracker/requirements.txt
          pip install -r tools/optimize_templates/requirements.txt

      - name: Download JSON data
        run: |
          python3 -u tools/map_tracker/map_fetcher.py json -o .cache/data

      - name: Get version info
        id: version
        run: |
          readarray -t VER < <(python3 tools/map_tracker/map_fetcher.py version -i .cache/data/version.json)
          echo "version=${VER[0]}" >> "$GITHUB_OUTPUT"
          echo "game_version=${VER[1]}" >> "$GITHUB_OUTPUT"
          echo "res_version=${VER[2]}" >> "$GITHUB_OUTPUT"

      - name: Download map images
        run: |
          python3 -u tools/map_tracker/map_fetcher.py image -i .cache/data -o .cache/images

      - name: Generate level maps
        run: |
          python3 -u tools/map_tracker/map_generator.py distinguish_levels -i .cache/images -o .cache/final --layout-dir .cache/data

      - name: Generate tier maps
        run: |
          python3 -u tools/map_tracker/map_generator.py tidy_tiers -i .cache/images -o .cache/final

      - name: Copy images to assets
        run: |
          cp -f .cache/final/*.png assets/resource/image/MapTracker/map/

      - name: Generate bbox data
        run: |
          python3 -u tools/map_tracker/map_generator.py bbox -i assets/resource/image/MapTracker/map -o assets/data/MapTracker

      - name: Setup oxipng
        uses: baptiste0928/cargo-install@v3
        with:
          crate: oxipng

      - name: Optimize png images
        run: |
          python3 tools/optimize_templates/optimize_templates.py

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v8
        with:
          token: ${{ secrets.MAAEND_BOT_TOKEN }}
          add-paths: |
            assets/resource/image/MapTracker/map/
            assets/data/MapTracker/
          commit-message: "feat(map): sync map image ${{ steps.version.outputs.version }}"
          branch: feat/sync-map-image
          title: "feat(map): sync map image ${{ steps.version.outputs.version }}"
          body: |
            ## Summary

            This PR automatically synchronized map images and bbox data from zmdmap.

            Please review the changes before merging it.

            **Version Information:**
            - Version: ${{ steps.version.outputs.version }}
            - Game Version: ${{ steps.version.outputs.game_version }}
            - Resource Version: ${{ steps.version.outputs.res_version }}
